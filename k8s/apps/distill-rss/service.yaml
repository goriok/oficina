apiVersion: v1
kind: Service
metadata:
  name: distill-rss
  namespace: distill-rss
  labels:
    app: distill-rss
spec:
  selector:
    app: distill-rss
  ports:
    - name: http
      port: 80
      targetPort: 8000
