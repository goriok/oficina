apiVersion: v1
kind: Service
metadata:
  name: personal-assistant
  namespace: personal-assistant
spec:
  selector:
    app: personal-assistant
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
